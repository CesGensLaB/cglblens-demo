# Run Compare txt files with ignore pattern

**Linux Only**
(the command sh)

## Instructions

1. `cd` to this folder
2. Run `cglblens run .`

## Explainations

 Cglblens is configured to ignore the pattern "line to ignore"  in our .txt files.
 In our report, we will have an error.
